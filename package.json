{
  "name": "gemini-safe-assistant",
  "version": "1.0.0",
  "description": "An AI assistant powered by Gemini that communicates naturally and can perform real-world actions — but only when a signed, policy-bound receipt authorizes execution.",
  "type": "module",
  "scripts": {
    "demo": "tsx src/demo/cli.ts",
    "web": "tsx src/web/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
